# 编译烧录实测（精简版）

> 本流程已经过验证，完全可靠，无任何问题
> 板型: **MakerGO ESP32 C3 SuperMini** | FQBN: `esp32:esp32:makergo_c3_supermini` | 分区: `no_ota`
> 串口: **COM4** | 项目: `D:\dev\sms_forwarding\code`

## 前置条件

- arduino-cli 已安装（位于 `C:\Program Files\Arduino IDE\resources\app\lib\backend\resources\arduino-cli.exe`）

## 一条命令编译

```powershell
$env:Path = "C:\Program Files\Arduino IDE\resources\app\lib\backend\resources;$env:Path"
$env:ARDUINO_DIRECTORIES_DATA = "D:\dev\arduino_pack"
$env:ARDUINO_DIRECTORIES_USER = "D:\dev\arduino_pack\user"
arduino-cli compile --fqbn esp32:esp32:makergo_c3_supermini --board-options PartitionScheme=no_ota --build-path "D:\dev\arduino_pack\build" "D:\dev\sms_forwarding\code"
```

> 首次编译约 3-5 分钟，耐心等待，不要提早结束。
> Wi-Fi 凭据由网页保存到设备 NVS，不需要创建或提交 `wifi_config.h`。

## 一条命令烧录

```powershell
arduino-cli upload --fqbn esp32:esp32:makergo_c3_supermini --board-options PartitionScheme=no_ota --port COM4 --input-dir "D:\dev\arduino_pack\build" "D:\dev\sms_forwarding\code"
```

## 查看日志

```powershell
arduino-cli monitor --port COM4 --config 115200
```
